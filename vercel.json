{
  "version": 2,
  "builds": [
    {
      "src": "routes.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      // Match all requests starting with /api
      "dest": "routes.py"
      // Direct to your routes file
    }
  ]
}
